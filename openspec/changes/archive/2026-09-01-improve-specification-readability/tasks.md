## 1. Structural validation

- [x] 1.1 `[change]` Add table-driven Node tests for a conforming Requirement, unknown and repeated blocks, block-order violations, unknown and missing Scenario tags, and Markdown inside fenced code or HTML comments; verify the tests fail because `tools/validate-spec-structure.mjs` does not yet exist.
- [x] 1.2 `[change]` Implement `tools/validate-spec-structure.mjs` as the responsibility-specific main-spec structure validator described in `design.md`, including optional repository-root input and file-and-line diagnostics; verify with `node --test tools/validate-spec-structure.test.mjs`.
- [x] 1.3 `[change]` Add the validator test and full-corpus validation commands to `.github/workflows/openspec-check.yml`; verify the workflow uses the same commands that pass locally.

## 2. Atomic Scenario retirement

- [x] 2.1 `[change]` Add archive workflow tests for a valid retirement, an absent manifest, unsupported version, duplicate entry, invalid capability, Requirement, or Scenario identity, capability without a corresponding delta, last-Scenario rejection, and rollback after post-archive validation failure; verify the valid-retirement test fails before publisher support is added and the existing no-manifest behavior remains covered.
- [x] 2.2 `[change]` Extend `tools/archive-change.mjs` to validate the versioned Change-local manifest, remove only exact named Scenario blocks after ordinary strict archive, require at least one remaining Scenario per Requirement, and restore both main specs and the active Change on failure; verify with `node --test tools/archive-change.test.mjs`.
- [x] 2.3 `[change]` Integrate structural corpus validation into the publisher's final atomic validation boundary without changing publication for Changes that omit the manifest; verify the rollback test observes the original main specs and active Change after a forced structural failure.

## 3. Specification methodology and corpus conformance

- [x] 3.1 `[change]` Adapt the latest `~/openspec-template` rules for reader-facing Conceptual Models, representation selection, Invariants, and example-only Scenarios into `openspec/specs/README.md`, `MODELING.md`, and `REVIEW.md` while preserving Arcloom terminology and publication rules; verify all three documents assign each rule to one consistent artifact and representation.
- [x] 3.2 `[change]` Apply the same guidance to `openspec/config.yaml`, `openspec/schemas/quality-spec/schema.yaml`, its `model.md` and `spec.md` templates, and `openspec/templates/main-spec.md`; verify generated artifact instructions forbid leaving normative rules only in Scenarios or `model.md`.
- [x] 3.3 `[change]` Upgrade `openspec/specs/_schema/requirement-structure.json` to the adapted version 2 structure, retaining the established Japanese labels, adding `不変条件`, representation metadata, canonical order, and the seven accepted Scenario tags; verify with the validator fixture suite and `openspec schema validate quality-spec`.
- [x] 3.4 `[change]` Create complete `MODIFIED Requirements` deltas for all eleven existing capabilities, add explicit renames for all thirty-six legacy numbered titles, normalize unsupported Requirement blocks, add allowed tags to every retained Scenario, and move every Scenario-only rule into the normative representation selected by its structure; verify all seventy-five accepted guarantees remain under stable kebab-case IDs and every existing Scenario outcome maps to a rule, table row, Invariant, or retained example.

## 4. Main-spec equivalence

- [x] 4.1 `[[reconciliation-control-loop/exact-target-request]]`, `[[reconciliation-control-loop/level-based-attempt]]`, `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]`, and `[[reconciliation-control-loop/explicit-control-directive]]` Compare every existing Scenario outcome with the replacement rule, table row, Invariant, or retained Scenario and correct any omission in the delta; verify each retired Scenario named in `scenario-retirements.json` is fully derivable from its rewritten Requirement.
- [x] 4.2 `[[reconciliation-control-loop/target-result-isolation]]`, `[[reconciliation-control-loop/failure-does-not-retry]]`, `[[reconciliation-control-loop/caller-lifecycle]]`, and `[[reconciliation-control-loop/disposable-control-state]]` Perform the same guarantee-preservation review, including publication/cancellation races and absence of implicit retries; verify every Requirement slug occurs exactly once and no remaining Scenario adds a normative result absent from its Requirement.
- [x] 4.3 `[[plan-reconciliation-loop/fresh-plan-observation]]`, `[[plan-reconciliation-loop/current-plan-assessment]]`, `[[plan-reconciliation-loop/read-only-plan-attempt]]`, and `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Perform the same guarantee-preservation review for binding, fresh observation, result classification, read-only behavior, and ordinary reentry; verify every retired Scenario, including the unsupported `[isolation]` example, is fully represented by a rule or Invariant before retirement.
- [x] 4.4 `[change]` Review both complete Conceptual Model replacements in `model.md` against their rewritten Requirements for vocabulary, state/classification completeness, relationships, and structural Invariants, without editing either main-spec Conceptual Model during Apply; verify no Requirement depends on an undefined material concept.
- [x] 4.5 `[change]` Review `authorization`, `codex-plan-control-assessment`, `github-plan-creation-dry-run`, `github-plan-representation-observation`, `github-plan-snapshot`, `plan-application-request`, `plan-control`, `plan-representation-reconciliation`, and `plan` against the same guarantee-preservation rule; verify each retained Scenario is representative and each retired Scenario is derivable from its Requirement.

## 5. Verification and publication readiness

- [x] 5.1 `[change]` Run `node --test tools/validate-spec-structure.test.mjs tools/archive-change.test.mjs` and resolve every failure.
- [x] 5.2 `[change]` Run `openspec schema validate quality-spec`, `openspec validate improve-specification-readability --strict`, `openspec validate --specs --strict`, and `openspec validate --changes --strict`; verify every command succeeds before publication.
- [x] 5.3 `[change]` Run `go test ./...`, `go test -race ./...`, `go vet ./...`, and `git diff --check`; verify the behavior-preserving documentation and tooling refactor introduces no product-code regression or malformed diff.
- [x] 5.4 `[change]` Review the final Change against `openspec/specs/REVIEW.md`, confirm no P0 or P1 issue remains, and confirm the Change is ready for atomic publication with `node tools/archive-change.mjs improve-specification-readability`.
