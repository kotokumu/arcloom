## 1. Baseline and Published Model

- [x] 1.1 `[design-only migration evidence]` Freeze the current non-archive result of `rg -l 'arcloom-plan:v1|decodePayload|payloadFor'`, classify every occurrence as production authority, current test or fixture authority, intentional compatibility input, or immutable archive history, and record the command and inventory in `verification/native-milestone-3.md` before implementation changes the baseline.
- [x] 1.2 `[[github-plan-creation-dry-run/github-plan-narrative]]`, `[[github-plan-representation-observation/native-narrative-meaning]]` Replace the reader-facing Conceptual Model text in both owning main specs with the approved `model.md` section 3-7 definitions, update `ARCHITECTURE.md` from payload-version ownership to native narrative grammar, framing, and legacy compatibility without changing the Adapter boundary, and verify the complete main-spec corpus with `openspec validate --specs --strict` and Markdown lint. Leave Requirement-delta publication to the official OpenSpec archive command.

## 2. Native Creation through TDD

- [x] 2.1 `[[github-plan-creation-dry-run/github-plan-narrative]]` Add failing public `NewCreationRequestPlan` tests with independently written exact literals for canonical Milestone, dated and undated Issue, Unicode, CRLF, leading and trailing whitespace, extra LF bytes, no preamble or marker, round-trip source values, and repeated byte-equal results; verify each Red failure is caused by the metadata-backed producer.
- [x] 2.2 `[[github-plan-creation-dry-run/explicit-github-target-and-representation]]`, `[[github-plan-creation-dry-run/validation-result]]` Add failing public table tests for every representability partition: each reserved H2 full line at the start, middle, and end of Goal or Acceptance Condition; an Acceptance Condition full line beginning `### `; accepted near misses; and simultaneous independent invalid inputs. Assert exact `UnsupportedRepresentation`, `RepresentationField`, and a zero Request Plan for structural collision.
- [x] 2.3 `[[github-plan-creation-dry-run/github-plan-narrative]]`, `[[github-plan-creation-dry-run/explicit-github-target-and-representation]]`, `[[github-plan-creation-dry-run/validation-result]]` Implement the smallest private native renderer and selected-scheme collision decision inside `providers/github/plan`, preserving exact Plan value bytes and existing validation contracts, and make the focused producer and representability tests pass before refactoring duplicated framing logic.
- [x] 2.4 `[[github-plan-creation-dry-run/milestone-creation-request-plan]]`, `[[github-plan-creation-dry-run/issue-creation-request-plan]]` Migrate public request-plan fixtures from payload content to exact native narratives while retaining Milestone 0/1/multiple-Task topology, Issue 0/100/101-Task boundaries, adjacent Task-Issue and Sub-issue pairs, result-kind references, optional dates, declared ordering, and no partial plan on failure; verify all creation tests pass with the race detector.

## 3. Native Representation Observation through TDD

- [x] 3.1 `[[github-plan-representation-observation/native-narrative-meaning]]` Add failing Controller-mediated black-box tests for canonical Milestone and Issue narratives, exact creation-output round trips, arbitrary non-marker preamble and its removal, legacy marker plus native content, conflicting payload plus native content, marker-only content, and removed-marker content; assert only native sections establish facts and the paired preamble-removal Results are equal.
- [x] 3.2 `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/conservative-failure-observation]]` Add failing public-result matrices for unavailable root text; missing, duplicate, out-of-order, reserved-in-value, and malformed required H2 structure; every wrong or malformed Acceptance ordinal and framing partition; canonical empty collections; every absent, valid, invalid, duplicate, out-of-order, and locally malformed Issue date partition; and a Milestone narrative `## Target Date` with a valid native `due_on`. Assert exact longest prefixes, Complete or Incomplete membership, localized Unavailable locations or violations, preserved independent facts, and no Provider detail.
- [x] 3.3 `[[github-plan-representation-observation/native-narrative-meaning]]` Implement one private call-local raw-source narrative fact establishment inside `providers/github/plan` that recognizes only the closed LF grammar, establishes structural text, collection completeness, and Issue date presence without performing Plan validation, and maps it through the existing representation Observation boundary; make the focused structural, compatibility, and exact-value tests pass without adding a public type, package, Port, Markdown parser, or mutable shared state.
- [x] 3.4 `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/milestone-representation-meaning]]`, `[[github-plan-representation-observation/issue-representation-meaning]]` Add and pass public Plan-value classification tests for empty or Unicode-whitespace Goal and members, duplicate conditions, valid and invalid mixed members, incomplete valid prefixes, invalid framed Issue dates, empty complete Sub-issues, closed assigned Issues, excluded Pull Requests, parent Pull Request roots, and absent or invalid Milestone `due_on`; verify existing Plan classifiers remain the sole owner of Plan validity.

## 4. Snapshot Parity and Retained Observation Guarantees

- [x] 4.1 `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/milestone-representation-meaning]]` Add failing public Snapshot Observer cases paired with the representation-observer source-authority matrix: valid native narrative, malformed required H2, incomplete ordinal prefix, invalid Goal or member, arbitrary and legacy preamble, conflicting payload, marker-only content, removed preamble or marker, and Milestone narrative Target Date. Assert current-Plan presence and exact value only for complete valid native meaning while coherent progress and native `due_on` remain independent.
- [x] 4.2 `[[github-plan-representation-observation/native-narrative-meaning]]` Change Snapshot current-Plan construction to consume the same private structural fact establishment used by representation observation, then refactor the two mappings so each public invocation remains independent and call-local while source authority, exact values, completeness, and compatibility meaning cannot diverge.
- [x] 4.3 `[[github-plan-representation-observation/milestone-representation-meaning]]`, `[[github-plan-representation-observation/issue-representation-meaning]]`, `[[github-plan-representation-observation/conservative-failure-observation]]` Keep the existing read-only target binding, zero/one/page Task boundaries, Task ordering and relationship behavior, pagination failure localization, cancellation and deadline outcomes, repeated-call freshness, and concurrent-call isolation green under `go test -race ./providers/github/plan`.

## 5. Payload Authority Removal

- [x] 5.1 `[[github-plan-creation-dry-run/github-plan-narrative]]`, `[[github-plan-representation-observation/native-narrative-meaning]]` After all native producer, representation, and Snapshot tests are green, remove production payload encoding, decoding, marker-specific interpretation, and obsolete payload-only tests; migrate retained tests and fixtures to native narratives or explicitly named compatibility inputs without adding fallback behavior.
- [x] 5.2 `[[github-plan-creation-dry-run/github-plan-narrative]]`, `[[github-plan-representation-observation/native-narrative-meaning]]` Re-run the frozen metadata-dependency search and verify zero production producer or decoder references, no current spec, fixture, or verification artifact treats the marker as authoritative, and every remaining non-archive occurrence is an intentional black-box compatibility input; record the command, result, allowlist, and reviewer decision in `verification/native-milestone-3.md`.

## 6. Verification and Completion Review

- [x] 6.1 `[[github-plan-creation-dry-run/explicit-github-target-and-representation]]`, `[[github-plan-creation-dry-run/github-plan-narrative]]`, `[[github-plan-creation-dry-run/milestone-creation-request-plan]]`, `[[github-plan-creation-dry-run/issue-creation-request-plan]]`, `[[github-plan-creation-dry-run/validation-result]]`, `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/milestone-representation-meaning]]`, `[[github-plan-representation-observation/issue-representation-meaning]]`, `[[github-plan-representation-observation/conservative-failure-observation]]` Run focused Red-Green-Refactor cycles and then `go test -v -race ./providers/github/plan`; resolve every creation, representation, Snapshot, lifecycle, and concurrency failure without weakening public assertions.
- [x] 6.2 `[[github-plan-representation-observation/conservative-failure-observation]]` Run the complete repository verification required by `docs/DEVELOPMENT.md`, including `go mod tidy -diff`, `go test -v -race ./...`, `go vet ./...`, and the configured Go lint workflow, and resolve every failure while preserving Provider-detail isolation and dependency direction.
- [x] 6.3 `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/milestone-representation-meaning]]` Add a deterministic no-network REST fixture for `kotokumu/arcloom` Milestone #3 with the recorded title, exact Goal, five ordered Acceptance Conditions, absent Target Date, and Tasks #47, #48, #57, and #58; verify the public Milestone Snapshot Observer establishes the exact current Plan and complete captured progress.
- [x] 6.4 `[[github-plan-representation-observation/native-narrative-meaning]]`, `[[github-plan-representation-observation/milestone-representation-meaning]]`, `[[github-plan-representation-observation/conservative-failure-observation]]` After the final implementation commit, run the opt-in authenticated public Snapshot proof against live Milestone #3 and record the secret-free command, UTC timestamp, GitHub API version, root and Task response SHA-256 hashes, exact observed Plan and progress, marker absence, freshness, drift verdict, and human reviewer identity in `verification/native-milestone-3.md`; treat access failure or drift as Deferred and not as SC-3 completion.
- [x] 6.5 `[design-only change verification]` Run `openspec validate remove-arcloom-metadata-from-github-plan --strict --no-interactive`, `openspec validate --specs --strict`, `npm run lint:markdown`, and `git diff --check`; resolve every error and confirm `PRODUCT.md` is unchanged and Requirement publication has not been manually duplicated outside the official archive workflow.
- [x] 6.6 `[design-only conformance review]` Obtain post-implementation architecture, SOLID responsibility, interface, procedural-code, code-quality, and Go test-specification reviews; resolve every Blocking and High finding, confirm no undesigned Concept, public contract, package, persistence, mutation, authorization, or generic Markdown responsibility was introduced, and trace each changed scenario to public tests or the approved external evidence protocol.
- [x] 6.7 `[design-only publication]` After implementation, evidence, and completion review are approved, have the change owner publish the Requirement deltas only through `openspec archive remove-arcloom-metadata-from-github-plan --yes`, run `npm run lint:openspec`, and verify the main specs contain the added `github-plan-narrative` and `native-narrative-meaning` Requirements, the modified creation and observation Requirements, and no remaining `versioned-plan-narrative` or `versioned-payload-meaning` Requirement.
