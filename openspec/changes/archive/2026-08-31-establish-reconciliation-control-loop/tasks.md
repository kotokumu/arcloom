## 1. Reconciliation Control Contracts

- [x] 1.1 `[[reconciliation-control-loop/exact-target-request]]` Add table-driven black-box tests for valid, empty, whitespace-only, leading/trailing Unicode whitespace, embedded whitespace, case-distinct, composed/decomposed Unicode, exact-byte preservation, equality, and zero-value Target Identity behavior; verify that invalid identities start no Attempt call.
- [x] 1.2 `[[reconciliation-control-loop/exact-target-request]]` Implement the immutable `reconciliationcontrol.TargetIdentity` value and constructors with the minimum code required to pass task 1.1.
- [x] 1.3 `[[reconciliation-control-loop/explicit-control-directive]]` Add table-driven tests for Await, Immediate, positive Delay, invalid Delay, accessors, and zero-value Directive behavior; verify that only constructed values are accepted.
- [x] 1.4 `[[reconciliation-control-loop/explicit-control-directive]]` Implement `Directive`, its explicit constructors, and validation with the minimum code required to pass task 1.3.
- [x] 1.5 `[[reconciliation-control-loop/target-result-isolation]]` Add compile-time and black-box tests in which two unrelated target fixtures implement only `Attempt[R]` and are passed to the same provided `Start`/Controller implementation, plus one successful Observation for which no semantic Reconciliation occurs; verify no target reimplements Controller lifecycle logic, values remain unchanged, control does not claim a Reconciliation Result exists, and no common Result taxonomy is required.
- [x] 1.6 `[[reconciliation-control-loop/target-result-isolation]]` Implement the generic `Attempt`, typed `Failure`, and immutable `Report` contracts with distinct Completion, Target Attempt Failed, and Control Directive Rejected construction paths; verify task 1.5 passes without reflection, `any` conversion, or a universal Reconciliation interface.

## 2. Reconciliation Controller Lifecycle

- [x] 2.1 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Add deterministic failing tests for duplicate Pending requests, requests during Active work, coincident completion and request, distinct-target progress, and zero/negative/positive concurrency bounds; use channel barriers rather than sleeps and do not require one Attempt per Pending request occurrence.
- [x] 2.2 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Implement `Start`, the concurrency-safe `Request` operation, and the minimal single-owner Controller scheduler needed for task 2.1; verify synchronous invalid-identity rejection, one Active Attempt per identity, and no lost active-period request under `go test -race`.
- [x] 2.3 `[[reconciliation-control-loop/level-based-attempt]]` Add a state-of-world test in which request causes differ while the Attempt receives only the same identity and reacquires changed current facts; verify no event payload, prior target value, or prior Directive crosses the Port.
- [x] 2.4 `[[reconciliation-control-loop/level-based-attempt]]` Complete identity-only request handling and current-attempt invocation with the minimum implementation required for task 2.3.
- [x] 2.5 `[[reconciliation-control-loop/explicit-control-directive]]` Add deterministic failing scheduler tests for Await, Immediate, constructed positive Delay, early external request during Delay, a returned zero Directive, and Directive commitment only after Completion delivery; use only public Directive construction plus a private package test time source and verify stale delay eligibility creates no duplicate attempt.
- [x] 2.6 `[[reconciliation-control-loop/explicit-control-directive]]` Implement explicit Directive scheduling and one private delay mechanism with the minimum code required for task 2.5; keep timer and queue mechanisms out of the public API.
- [x] 2.7 `[[reconciliation-control-loop/failure-does-not-retry]]` Add a barrier/table test proving that a non-nil Target Attempt error outranks any returned value and valid/invalid Directive, while nil error plus invalid Directive becomes Control Directive Rejected; verify exact target, absent Completion/Directive, `errors.Is` cause preservation, no implicit retry, preserved Active-period request, and no cross-target contamination.
- [x] 2.8 `[[reconciliation-control-loop/failure-does-not-retry]]` Implement the typed failure transitions with no inferred scheduling and verify task 2.7 under the race detector.
- [x] 2.9 `[[reconciliation-control-loop/caller-lifecycle]]` Add public black-box tests for publication-first and cancellation-first prospective Report cases plus a repeated simultaneous consumer-ready/cancellation race that accepts either winner but requires XOR: exactly one Report with committed Directive, or no Report and no Directive-based Attempt. Add normal mixed-target publication proving every returned outcome appears once without loss/duplication while asserting no cross-target order. Also test repeated/concurrent Reports and Wait calls; zero Controller behavior; nil/already-done/blocked submission context; accepted request followed by submission-context cancellation; simultaneous submission/lifecycle cancellation precedence; Report backpressure through observable no-new-start/accepted-request/existing-return/cancellable behavior; stopped Report consumer; and an Attempt returning nominal success only after `ctx.Done`. Verify no post-stop Completion and use timeout only as a deadlock guard.
- [x] 2.10 `[[reconciliation-control-loop/caller-lifecycle]]` Implement stable Report stream identity, request-context precedence, interruptible exact-once normal Report publication, stop, Active Attempt cancellation and waiting, Report closure, and `Wait` with the minimum code required for task 2.9; do not represent publication as delivery to a semantic Result Destination.
- [x] 2.11 `[[reconciliation-control-loop/disposable-control-state]]` Add a restart-style test that discards one Controller and starts a new Controller from a new request and changed current facts without restoring prior requests, outcomes, or delay state.
- [x] 2.12 `[[reconciliation-control-loop/disposable-control-state]]` Keep all mutable scheduling and unpublished Report state scoped to one started Controller and verify task 2.11 without a repository, checkpoint, lease, leader, or durable queue abstraction.

## 3. Read-Only Plan Attempt

- [x] 3.1 `[[plan-reconciliation-loop/fresh-plan-observation]]` Generate the Go table-test scaffold and add failing cases for invalid PlanTarget identity or missing observation boundary; invalid accepted kind, nil Resolver, or nil Assessor setup; wrong requested kind; Resolver error; invalid or identity-mismatched resolved binding; two identities with distinct sentinel bindings; current/no-current Snapshot; observation failure; and caller cancellation before/during/after Resolver and Snapshot return. Verify setup errors occur before an Attempt exists, exact Resolver context/identity and binding identity, cooperative Resolver cancellation within its declared bound, wrong-kind Resolver exclusion, runtime FailureCode/non-unwrapping rules, zero AttemptResult/Directive on Attempt error, exactly one Observer invocation, and skipped later boundaries on every earlier failure or no-current-Plan success.
- [x] 3.2 `[[plan-reconciliation-loop/fresh-plan-observation]]` Implement self-identifying validated `PlanTarget`, synchronous setup error sentinels, runtime Plan Attempt failures, immutable discriminated `AttemptResult`, and `NewAttempt` binding/Snapshot branches with the minimum code required for task 3.1; verify no-current Plan establishes `CurrentPlanNotEstablished`, exposes no Assessment, and performs no semantic Reconciliation.
- [x] 3.3 `[[plan-reconciliation-loop/current-plan-assessment]]` Add ordered table-driven failing tests for Delivery Observation failure, Complete, Retain, Revise, Insufficient Information, assessment failure, and cancellation before/after Delivery and Assessment return. Verify Snapshot→Delivery→Assessment order, exact same-binding sentinels, exact current Plan and observations, stable FailureCode/non-unwrapping rules, existing Snapshot/Plan Control error preservation, context-error precedence, zero AttemptResult/Directive on error, unchanged valid Assessment, and `CurrentPlanAssessed` as the only branch that records semantic Plan Reconciliation.
- [x] 3.4 `[[plan-reconciliation-loop/current-plan-assessment]]` Implement the Delivery Observer and `CurrentPlanAssessed` branch by composing only the existing `plansnapshot` and `plancontrol` public contracts; normalize only Plan-owned boundary failures and verify task 3.3.
- [x] 3.5 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Add black-box and dependency tests proving that both successful Plan Attempt Result branches select Await Another Request and that `planreconciliation` imports or invokes no Authorization, Plan Application Request, Actor, Provider, or Task-execution contract.
- [x] 3.6 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Complete the Plan Module's direct generic Attempt implementation and read-only boundary with the minimum code required for task 3.5; retain any Proposed Plan only inside the existing Assessment and document that Provider observation/AI operational effects are not target mutation.
- [x] 3.7 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Add a two-attempt integration test in which an external interaction occurs between explicit identity requests; verify the second attempt performs fresh observation and receives no event, receipt, application result, or prior result input.
- [x] 3.8 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Wire the Plan Attempt to the generic Controller through its narrow Attempt Port and verify that an external interaction without a later Request starts no Attempt.

## 4. Architecture and Conformance

- [x] 4.1 `[[plan-representation-reconciliation/evidence-derived-immutable-result]]` Preserve the existing Plan Representation black-box cases for empty, Difference-only, unavailable-only, mixed, zero-value, input-slice mutation, and accessor-slice mutation behavior; add a dependency check that initially fails because `planrepresentation` imports the standalone `reconciliation` package.
- [x] 4.2 `[[plan-representation-reconciliation/evidence-derived-immutable-result]]` Move `Determination`, its three values, and immutable Evidence snapshot ownership into `planrepresentation`, update repository consumers, and remove the standalone `reconciliation` package; verify all preserved PRR-6 behavior and the dependency check pass without a compatibility facade.
- [x] 4.3 `[change]` Design-only conformance check: verify package names and exported symbols name the owning concepts (`planrepresentation`, `reconciliationcontrol`, `planreconciliation`, Determination, TargetIdentity, Directive, ErrInvalidDirective, Failure, Report, Attempt, Controller, PlanTarget, AttemptResult, NewAttempt) and introduce no `helper`, `util`, `shared`, `common`, `manager`, `handler`, `processor`, or `runtime` responsibility bucket.
- [x] 4.4 `[[reconciliation-control-loop/target-result-isolation]]` Verify dependency direction: `reconciliationcontrol` uses only the Go standard library and never imports target-specific Modules; record the package dependency output.
- [x] 4.5 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Verify dependency direction: `planreconciliation` depends only on `reconciliationcontrol`, `plansnapshot`, `plancontrol`, and the standard library; record the package dependency output.
- [x] 4.6 `[change]` Design-only architecture gate: compare implementation boundaries with `ARCHITECTURE.md` and `design.md`, run the Architecture, interface, test, procedural-code, SOLID, and code-quality reviews required for a high-risk Change, and resolve every P0/P1 finding before completion.
- [x] 4.7 `[change]` Design-only bounded-state conformance: inspect the Controller implementation and record that exactly one scheduler owner retains at most one Pending Publication Report and that Active-return buffering is bounded by the configured concurrency limit; do not assert private queue shape or channel capacity from behavior tests.

## 5. Verification

- [x] 5.1 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Execute at least 100 mixed-target requests and verify no same-target overlap, lost active-period request, cross-target result contamination, concurrency-bound violation, or race detector finding with `go test -race ./...`.
- [x] 5.2 `[[reconciliation-control-loop/caller-lifecycle]]` Execute cancellation behavior with test-owned barriers and wait groups plus a generous one-second deadlock guard; record that after all Active Attempts return, Reports closes and `Wait` exposes the caller context error without waiting for Report consumption, Pending or Delayed work, or another timer.
- [x] 5.3 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Resolve the opt-in real Plan proof gate: no new external application interaction was authorized for this Change apply, so no live mutation was performed; retain the prior operator-owned proof as external evidence and verify the new explicit-request/fresh-observation semantics through the Plan Controller integration test.
- [x] 5.4 `[change]` Run `go test ./...`, `go test -race ./...`, `go vet ./...`, repository linters, documentation validation, and `openspec validate establish-reconciliation-control-loop --strict`; resolve every failure and record the exact commands and outcomes.

## Verification Record — 2026-08-31

- Public-name conformance: `go doc` for `reconciliationcontrol` and `planreconciliation` exposes the approved owning concepts. Source inspection found no newly introduced `helper`, `util`, `shared`, `common`, `manager`, `handler`, `processor`, or `runtime` responsibility bucket.
- Dependency output:
  - `reconciliationcontrol -> context, errors, fmt, strings, time`
  - `planreconciliation -> context, errors, fmt, plancontrol, plansnapshot, reconciliationcontrol`
  - `planrepresentation -> context, errors, fmt, plan, sort`
- Bounded-state inspection: `Controller.run` is the only scheduler-state owner; its single `prospective *prospectiveReport` retains at most one unpublished Report, and `returns := make(chan attemptReturn, c.maximum)` bounds Active-return buffering by the configured concurrency limit.
- Final independent Architecture/SOLID, interface, test/model/procedural, and code-quality reviews reported no unresolved P0/P1 findings. Review-driven fixes added the public substitution and context contracts, corrected nil-context classification, made cancellation win over an Attempt return observed after lifecycle end, added the missing backpressure and boundary-cancellation tests, preserved full Snapshot/Assessment evidence, and removed stopping-state busy-spin.
- `TestControllerSchedulingMixedRequestStress` submits 120 mixed requests after four Active requests and verifies same-target exclusion, preserved active-period eligibility, the concurrency bound, and exact target results. `go test -race ./...` passed.
- Cancellation tests use Attempt-owned barriers and one-second deadlock guards. They verify prospective Report discard, stopped-consumer termination, Active Attempt waiting, Report closure, caller context outcome, and no post-stop nominal Completion.
- The live proof remains opt-in because a new external Actor interaction requires exact operator authorization. The prior disposable proof is recorded at `openspec/changes/archive/2026-08-30-control-real-github-plan/verification/real-plan-proof.md`; this Change's `TestPlanAttemptControllerReentryAlwaysObservesFreshFacts` verifies that the new loop receives no receipt/event payload and observes again only after an explicit identity Request.
- Passed commands:
  - `go test ./...`
  - `go test -race ./...`
  - `go vet ./...`
  - `go mod tidy -diff`
  - `golangci-lint run ./...` (`0 issues`)
  - `openspec validate --specs --strict` (9 passed)
  - `openspec validate --changes --strict` (1 passed)
  - `openspec validate establish-reconciliation-control-loop --strict`
