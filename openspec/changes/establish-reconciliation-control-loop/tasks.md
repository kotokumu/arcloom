## 1. Reconciliation Control Contracts

- [ ] 1.1 `[[reconciliation-control-loop/exact-target-request]]` Add table-driven black-box tests for valid, empty, whitespace-only, leading/trailing Unicode whitespace, embedded whitespace, case-distinct, composed/decomposed Unicode, exact-byte preservation, equality, and zero-value Target Identity behavior; verify that invalid identities start no Reconciler call.
- [ ] 1.2 `[[reconciliation-control-loop/exact-target-request]]` Implement the immutable `reconciliationcontrol.TargetIdentity` value and constructors with the minimum code required to pass task 1.1.
- [ ] 1.3 `[[reconciliation-control-loop/explicit-control-directive]]` Add table-driven tests for Await, Immediate, positive Delay, invalid Delay, accessors, and zero-value Directive behavior; verify that only constructed values are accepted.
- [ ] 1.4 `[[reconciliation-control-loop/explicit-control-directive]]` Implement `Directive`, its explicit constructors, and validation with the minimum code required to pass task 1.3.
- [ ] 1.5 `[[reconciliation-control-loop/target-result-isolation]]` Add compile-time and black-box tests using two Controller instances with unrelated result types and success/failure Reports; verify that result values remain unchanged and no common result taxonomy is required.
- [ ] 1.6 `[[reconciliation-control-loop/target-result-isolation]]` Implement the generic `Reconciler`, semantic typed `Failure`, and immutable `Report` contracts with distinct success, Target Attempt Failed, and Control Directive Rejected construction paths; verify task 1.5 passes without reflection or `any` result conversion.

## 2. Reconciliation Controller Lifecycle

- [ ] 2.1 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Add deterministic failing tests for duplicate Pending requests, requests during Active work, coincident completion and request, distinct-target progress, and zero/negative/positive concurrency bounds; use channel barriers rather than sleeps and do not require one Attempt per Pending request occurrence.
- [ ] 2.2 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Implement `Start`, the concurrency-safe `Request` operation, and the minimal single-owner Controller scheduler needed for task 2.1; verify synchronous invalid-identity rejection, one Active Attempt per identity, and no lost active-period request under `go test -race`.
- [ ] 2.3 `[[reconciliation-control-loop/level-based-attempt]]` Add a state-of-world test in which request causes differ while the Reconciler receives only the same identity and reacquires changed current facts; verify no event payload, prior result, or prior Directive crosses the Port.
- [ ] 2.4 `[[reconciliation-control-loop/level-based-attempt]]` Complete identity-only request handling and current-attempt invocation with the minimum implementation required for task 2.3.
- [ ] 2.5 `[[reconciliation-control-loop/explicit-control-directive]]` Add deterministic failing scheduler tests for Await, Immediate, constructed positive Delay, early external request during Delay, a returned zero Directive, and Directive commitment only after Completion delivery; use only public Directive construction plus a private package test time source and verify stale delay eligibility creates no duplicate attempt.
- [ ] 2.6 `[[reconciliation-control-loop/explicit-control-directive]]` Implement explicit Directive scheduling and one private delay mechanism with the minimum code required for task 2.5; keep timer and queue mechanisms out of the public API.
- [ ] 2.7 `[[reconciliation-control-loop/failure-does-not-retry]]` Add a barrier/table test proving that a non-nil Target Attempt error outranks any returned result and valid/invalid Directive, while nil error plus invalid Directive becomes Control Directive Rejected; verify exact target, absent Completion/Directive, `errors.Is` cause preservation, no implicit retry, preserved Active-period request, and no cross-target contamination.
- [ ] 2.8 `[[reconciliation-control-loop/failure-does-not-retry]]` Implement the typed failure transitions with no inferred scheduling and verify task 2.7 under the race detector.
- [ ] 2.9 `[[reconciliation-control-loop/caller-lifecycle]]` Add public black-box tests for delivery-first and cancellation-first prospective Report cases plus a repeated simultaneous consumer-ready/cancellation race that accepts either winner but requires XOR: exactly one Report with committed Directive, or no Report and no Directive-based Attempt. Add normal mixed-target delivery proving every returned outcome appears once without loss/duplication while asserting no cross-target order. Also test repeated/concurrent Reports and Wait calls; zero Controller behavior; nil/already-done/blocked submission context; accepted request followed by submission-context cancellation; simultaneous submission/lifecycle cancellation precedence; Report backpressure through observable no-new-start/accepted-request/existing-return/cancellable behavior; stopped Report consumer; and a Reconciler returning nominal success only after `ctx.Done`. Verify no post-stop Completion and use timeout only as a deadlock guard.
- [ ] 2.10 `[[reconciliation-control-loop/caller-lifecycle]]` Implement stable Report stream identity, request-context precedence, interruptible exact-once normal Report delivery, stop, Active Attempt cancellation and waiting, Report closure, and `Wait` with the minimum code required for task 2.9.
- [ ] 2.11 `[[reconciliation-control-loop/disposable-control-state]]` Add a restart-style test that discards one Controller and starts a new Controller from a new request and changed current facts without restoring prior requests, outcomes, or delay state.
- [ ] 2.12 `[[reconciliation-control-loop/disposable-control-state]]` Keep all mutable scheduling and undelivered Report state scoped to one started Controller and verify task 2.11 without a repository, checkpoint, lease, leader, or durable queue abstraction.

## 3. Read-Only Plan Reconciliation Attempt

- [ ] 3.1 `[[plan-reconciliation-loop/fresh-plan-observation]]` Generate the Go table-test scaffold and add failing cases for invalid PlanTarget identity or missing observation boundary; invalid accepted kind, nil Resolver, or nil Assessor setup; wrong requested kind; Resolver error; invalid or identity-mismatched resolved binding; two identities with distinct sentinel bindings; current/no-current Snapshot; observation failure; and caller cancellation before/during/after Resolver and Snapshot return. Verify setup errors occur before a Reconciler exists, exact Resolver context/identity and binding identity, cooperative Resolver cancellation within its declared bound, wrong-kind Resolver exclusion, runtime FailureCode/non-unwrapping rules, zero Result/Directive on Attempt error, exactly one Observer invocation, and skipped later boundaries on every earlier failure or no-current-Plan success.
- [ ] 3.2 `[[plan-reconciliation-loop/fresh-plan-observation]]` Implement self-identifying validated `PlanTarget`, synchronous setup error sentinels, runtime Plan Attempt failures, immutable `Result`, and `NewReconciler` binding/Snapshot branches with the minimum code required for task 3.1.
- [ ] 3.3 `[[plan-reconciliation-loop/current-plan-assessment]]` Add ordered table-driven failing tests for Delivery Observation failure, Complete, Retain, Revise, Insufficient Information, assessment failure, and cancellation before/after Delivery and Assessment return. Verify Snapshot→Delivery→Assessment order, exact same-binding sentinels, exact current Plan and observations, stable FailureCode/non-unwrapping rules, existing Snapshot/Plan Control error preservation, context-error precedence, zero Result/Directive on error, and unchanged valid Assessment.
- [ ] 3.4 `[[plan-reconciliation-loop/current-plan-assessment]]` Implement the Delivery Observer and optional Assessment branches by composing only the existing `plansnapshot` and `plancontrol` public contracts; normalize only Plan-owned boundary failures and verify task 3.3.
- [ ] 3.5 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Add black-box and dependency tests proving that every successful Plan result selects Await Another Request and that `planreconciliation` imports or invokes no Authorization, Plan Application Request, Actor, Provider, or Task-execution contract.
- [ ] 3.6 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Complete the Plan Module's direct generic Reconciler implementation and read-only boundary with the minimum code required for task 3.5; retain any Proposed Plan only inside the existing Assessment and document that Provider observation/AI operational effects are not target mutation.
- [ ] 3.7 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Add a two-attempt integration test in which an external interaction occurs between explicit identity requests; verify the second attempt performs fresh observation and receives no event, receipt, application result, or prior result input.
- [ ] 3.8 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Wire the Plan attempt to the generic Controller through its narrow Reconciler Port and verify that an external interaction without a later request starts no attempt.

## 4. Architecture and Conformance

- [ ] 4.1 `[change]` Design-only conformance check: verify package names and exported symbols name the owning concepts (`reconciliationcontrol`, `planreconciliation`, TargetIdentity, Directive, Failure, Report, Controller, PlanTarget, Result, NewReconciler) and introduce no `helper`, `util`, `shared`, `common`, `manager`, `handler`, `processor`, or `runtime` responsibility bucket.
- [ ] 4.2 `[[reconciliation-control-loop/target-result-isolation]]` Verify dependency direction: `reconciliationcontrol` uses only the Go standard library and never imports target-specific Modules; record the package dependency output.
- [ ] 4.3 `[[plan-reconciliation-loop/read-only-plan-attempt]]` Verify dependency direction: `planreconciliation` depends only on `reconciliationcontrol`, `plansnapshot`, `plancontrol`, and the standard library; record the package dependency output.
- [ ] 4.4 `[change]` Design-only architecture gate: compare implementation boundaries with `ARCHITECTURE.md` and `design.md`, run the Architecture, interface, test, procedural-code, SOLID, and code-quality reviews required for a high-risk Change, and resolve every P0/P1 finding before completion.
- [ ] 4.5 `[change]` Design-only bounded-state conformance: inspect the Controller implementation and record that exactly one scheduler owner retains at most one Pending Delivery Report and that Active-return buffering is bounded by the configured concurrency limit; do not assert private queue shape or channel capacity from behavior tests.

## 5. Verification

- [ ] 5.1 `[[reconciliation-control-loop/per-target-exclusion-and-coalescing]]` Execute at least 100 mixed-target requests and verify no same-target overlap, lost active-period request, cross-target result contamination, concurrency-bound violation, or race detector finding with `go test -race ./...`.
- [ ] 5.2 `[[reconciliation-control-loop/caller-lifecycle]]` Execute cancellation behavior with test-owned barriers and wait groups plus a generous one-second deadlock guard; record that after all Active Reconcilers return, Reports closes and `Wait` exposes the caller context error without waiting for Report consumption, Pending or Delayed work, or another timer.
- [ ] 5.3 `[[plan-reconciliation-loop/ordinary-plan-reentry]]` Run an opt-in real Plan proof that records a fresh Snapshot, Plan Assessment, any separately authorized application interaction, an explicit later request, and a later fresh Snapshot without treating a receipt as target state.
- [ ] 5.4 `[change]` Run `go test ./...`, `go test -race ./...`, `go vet ./...`, repository linters, documentation validation, and `openspec validate establish-reconciliation-control-loop --strict`; resolve every failure and record the exact commands and outcomes.
