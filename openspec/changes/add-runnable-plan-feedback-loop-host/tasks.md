## 1. Specification Publication Preparation

- [ ] 1.1 `[change]` Create the reader-facing Conceptual Model for the new `plan-feedback-loop-operation` main spec from `model.md`, including operation and delivery states, the Report classification table, and structural invariants; verify with `npm run lint:openspec` before implementation and again after official archival.

## 2. Exact Plan Host Configuration

- [ ] 2.1 `[[plan-feedback-loop-operation/exact-plan-operation-configuration]]` Add failing external-package tests for a zero binding, invalid Plan Target, nil assessor, internal construction rejection, and valid exact single-target construction, then implement the minimum immutable `planreconciliation.PlanAttemptBinding` whose constructor alone associates the target's exact identity with its configured Attempt. Verify every invalid partition returns the zero Binding and `ErrInvalidPlanAttemptBinding` without observation or assessment; verify the valid Binding exposes the exact target identity and its Attempt rejects a different requested identity before observation. GitHub issue: #53.
- [ ] 2.2 `[[plan-feedback-loop-operation/exact-plan-operation-configuration]]` Add failing startup tests for nil context, already-ended lifecycle, zero Plan Attempt Binding, nil destination, and complete exact-target configuration, then implement provisional `internal/planhost.Start` with the documented validation precedence and stable errors. Verify each rejection creates no goroutine or stream and performs no observation, assessment, Request, Report, or delivery work. GitHub issues: #53 and #56.

## 3. Explicit Trigger and Report Routing

- [ ] 3.1 `[[plan-feedback-loop-operation/explicit-plan-trigger]]` Add failing tests for initial, later, duplicate, active-time, cancelled-submission, and post-stop triggers, then implement `Host.Trigger` as a payload-free Request for only the configured identity; verify behavior without asserting private scheduling order. GitHub issues: #53 and #56.
- [ ] 3.2 `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Add failing tests that consume each exact Controller Report once, preserve Controller order, invoke the destination only for Current Plan Assessed, and expose the same Report as processed only after destination success; then implement the sole-consumer report pump and one-entry processed Host Report buffer. Verify Current Plan Not Established and both Controller Failure kinds cause zero semantic deliveries. GitHub issues: #53 and #56.
- [ ] 3.3 `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Add failing exact-value and equal-value-per-distinct-Report tests, then implement the Plan-specific destination invocation with the original Target Identity and exact `plancontrol.Assessment`; verify Complete, Retain, Revise, and Insufficient Information are delivered without Snapshot exposure or interpretation, once per qualifying Report, and without Authorization, application, Task execution, or mutation. GitHub issue: #56.

## 4. Delivery Failure and Lifecycle

- [ ] 4.1 `[[plan-feedback-loop-operation/delivery-failure-and-fresh-reentry]]` Add failing tests for destination success, destination failure after a recorded possible destination effect, and a newly started Host after failure; then implement fail-stop delivery, an unwrap-capable Plan-specific `DeliveryError`, and no retry or replay. Verify the failed Controller Report is not exposed as processed, the Planning Context revision is unchanged by Host/destination, and the new Host obtains a fresh Snapshot with no prior cycle facts. GitHub issues: #51, #53, and #56.
- [ ] 4.2 `[[plan-feedback-loop-operation/caller-scoped-operation-lifecycle]]` Add channel-controlled failing tests for cancellation while idle, during Request submission, observation, processed-Report backpressure, assessment, and destination delivery; then implement coordinated child cancellation, Report closure, and `Wait`. Verify shutdown waits only for active boundaries and uses no sleep, polling, or stopped Report consumer. GitHub issues: #51 and #53.
- [ ] 4.3 `[[plan-feedback-loop-operation/caller-scoped-operation-lifecycle]]` Add deterministic terminal tests: cancel-first yields no processed Report and the lifecycle error; success-first yields the processed Report, then caller cancellation and the lifecycle error; failure-first yields no processed Report and a stable `DeliveryError`. Verify repeated cancellation, zero Host behavior, post-stop `Trigger`, and multiple concurrent `Wait` calls preserve the same terminal meaning before using `go test -race` only as an additional data-race check. GitHub issue: #51.

## 5. Deterministic Local Feedback-Loop Environment

- [ ] 5.1 `[[plan-feedback-loop-operation/explicit-plan-trigger]]` and `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Build specifically named test-local controlled Planning Context, External Actor operations, fact-derived assessor, and recording Plan Result Destination in the external integration test; verify credentials, network, manual changes, wall-clock sleeps, and production-only test abstractions are absent. GitHub issue: #51.
- [ ] 5.2 `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Add the S0(all Open)/Retain → S1(one Closed)/Retain → S2(all Closed)/Complete convergence test driven by two named External Actor changes. Verify three distinct source-owned revisions, Snapshot-before-delivery-observation ordering, exact state-derived assessment inputs, one delivery per assessed Report, processed Report after delivery, and mutation history containing only those Actor changes. GitHub issue: #52.
- [ ] 5.3 `[[plan-feedback-loop-operation/delivery-failure-and-fresh-reentry]]` Extend the deterministic environment with same-Host no-current recovery, same-Host Attempt Failure recovery, duplicate trigger, trigger during In Flight delivery, destination failure, and cancellation scenarios; verify each uses fresh current revision, exercises the same Host contract, and never mutates Planning Context from the Host, assessor, or destination. GitHub issues: #51 and #52.

## 6. Real GitHub and Codex Composition

- [ ] 6.1 `[[plan-feedback-loop-operation/exact-plan-operation-configuration]]` Add a command-local GitHub binding adapter that constructs one exact Milestone target from existing `githubplan` observers and selects only provider-independent Progress Evidence, then add the thin `cmd/arcloom-plan` Composition Root that creates `PlanAttemptBinding` and wires the provisional Host. Verify `main` only parses configuration and wires dependencies, GitHub mapping stays in `githubplan`, and Result classification stays in `internal/planhost`. GitHub issues: #53 and #54.
- [ ] 6.2 `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Bind `codexplancontrol.NewAssessor` through `codexappserver.Client` and add a Plan-specific terminal destination; verify Host code contains no process, stdio, JSON-RPC, correlation, protocol, or shutdown implementation and that unsafe Codex configuration starts no Turn. GitHub issues: #55 and #56.
- [ ] 6.3 `[[plan-feedback-loop-operation/caller-scoped-operation-lifecycle]]` Add command-level tests with injected HTTP and Codex Client boundaries for successful one-cycle execution, no-current Report, GitHub failure, Codex failure, destination failure, and OS cancellation; verify command exit behavior without network or a real Codex process. GitHub issues: #54, #55, and #56.
- [ ] 6.4 `[[plan-feedback-loop-operation/explicit-plan-trigger]]` and `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]` Execute the real verification matrix against GitHub Milestone #2 initially and after at least two named provider-native Task-progress changes, using Codex only through the Go SDK. Record operator/time, pre-run Task states, exact Report/Assessment classifications, destination completion, exit outcome, and native references in a disposable verification record without secrets, Arcloom metadata, prose-matching assertions, or authoritative state claims. GitHub issues: #44, #54, #55, and #56.

## 7. Verification and Review

- [ ] 7.1 `[[plan-feedback-loop-operation/exact-plan-operation-configuration]]`, `[[plan-feedback-loop-operation/explicit-plan-trigger]]`, `[[plan-feedback-loop-operation/assessed-plan-result-delivery]]`, `[[plan-feedback-loop-operation/delivery-failure-and-fresh-reentry]]`, and `[[plan-feedback-loop-operation/caller-scoped-operation-lifecycle]]` Run all specified happy, error, boundary, concurrency, and idempotency scenarios with `go test ./...` and `go test -race ./...`; record that no test relies on wall-clock sleep for correctness.
- [ ] 7.2 `[design]` Review the implemented responsibility assignment for procedure-centered Host logic, duplicated Controller policy, Provider DTO/error leakage, generic Result abstractions, and ambiguous `helper`, `util`, `shared`, `common`, `manager`, or `processor` ownership; refactor only within the approved #2 design or return to the DesignDoc when a new boundary is required.
- [ ] 7.3 `[change]` Run `npm run lint:markdown` and `openspec validate add-runnable-plan-feedback-loop-host --strict --no-interactive`; resolve every P0 and P1 specification or design finding.
- [ ] 7.4 `[change]` Use `openspec archive add-runnable-plan-feedback-loop-host --yes` to publish Requirement deltas, run `npm run lint:openspec`, and verify the new main spec retains the complete reader-facing Conceptual Model.
- [ ] 7.5 `[change]` Update GitHub Milestone #2 evidence and close only tasks whose implementation and verification are complete; leave Reconciliation remodelling, refactoring, and final Module/Interface decisions assigned to Milestone #3.
